[package]
name = "spacer-core"
version = "0.1.0"
edition = "2021"
description = "Core domain library for the Spacer project management tool"
license = "MIT"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
